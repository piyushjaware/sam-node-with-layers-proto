/**
 * Creates a lambda response
 */
exports.response = (statusCode, body) => {
    let response = {
        headers: {
            'Content-Type': 'application/json'
        },
        statusCode: statusCode,
        body: JSON.stringify(body)
    };
    return response;
};

